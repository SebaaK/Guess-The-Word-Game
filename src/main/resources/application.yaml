spring:
  datasource:
    url: jdbc:mysql://localhost:3306/JC_GWG?serverTimezone=Europe/Warsaw&useSSL=False&allowPublicKeyRetrieval=true
    username: jc
    password: jc
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  jackson:
    default-property-inclusion: non_null
  servlet:
    multipart:
      max-file-size: 10MB

game:
  difficultyLevelThreshold: 6

jwt:
  secret: long_secret_code
  expiration: 60